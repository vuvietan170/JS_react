justify-content: sap xep theo chieu ngang(flex-start,end , center, space-between,around) x
align-items: sap xep theo chieu doc(nhu tren)
flex-direction: column, row(reverse) dat cac hang muc theo hang theo cot x
 order: chi so cho hang muc rieng le x
align-self: hang muc rieng biet (thao tac giong align-items) x
flex-wrap: nowrap don theo hang ,wrap cac hang muc cuon xung quanh de tao them dong(reverse thi nguoc lai) x
flex-flow: gop thuoc tinh cua flex-direction va flex-wrap voi nhau x
align-content : khoang cach giua cac dong (stretch: keo dai dong phu hop voi hop chua)

# HTML
- Basics of HTML: how to write HTML code, and return the website
- HTML attributes: modify the behaviour of an element
- Syntax: which are the rules we had to follow when writing our code
- HTML structure (<!DOCTYPE html>,html,head,body,closed html tag)
---
# CSS
- Create element with HTML
- Style with css one-by-one (margin, background-color, padding,....)
- Hover,Transitions,Shadow
- Chrome dev tools and CSS box Model (style,computed)
- Text style (html entity: check mark,greater than,....)
- Nested layouts
- Display: block/ inline-block
- Grid
- Flexbox(navigation,sidebar)
- Position (absolute, fixed,relative)
# icons
- fontawesome.com
- font.google.com